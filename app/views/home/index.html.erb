<% if user_signed_in?%>
  <h4>Hello User, Welcome <%= current_user.email%></h4>
  <br/>
  <%= link_to "Sign Out", destroy_user_session_path,:method => :delete%>
<% else %>
  Logged out
  <%= link_to "Sign In", new_user_session_path%> or 
  <%= link_to "Sign Up", new_user_registration_path%> 
<% end %>
